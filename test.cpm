#include <stdio.h>

typedef struct {
    int  x;
    int  y;
    method int init(int x, int y) {
        this->x = 10;
        this->y = 10;
    }
    method int sum() {return this->x+this->y}
} Point;


int main(void) {
    Point p = .init(10,10);
    printf("%d\n", p.sum());
    return 0;
}
